import 'package:flutter/material.dart';

class StudySITPage extends StatelessWidget {
  const StudySITPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Study SIT method'),
      ),
      body: const Center(
        child: Text('Study SIT'),
      ),
    );
  }
}
