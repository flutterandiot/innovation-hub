import 'package:flutter/material.dart';

class IdeasContainer extends StatelessWidget {
  const IdeasContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Center(
        child: Text('Idea Container'),
      ),
    );
  }
}
